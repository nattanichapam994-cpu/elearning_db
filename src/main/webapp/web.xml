<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
         http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
         version="3.1">

  <display-name>LearnHub E-Learning</display-name>

  <!-- ====================================================
       CHARACTER ENCODING FILTER
       บังคับ UTF-8 ทุก request/response — สำคัญมากสำหรับภาษาไทย
       ==================================================== -->
  <filter>
    <filter-name>EncodingFilter</filter-name>
    <filter-class>org.apache.catalina.filters.SetCharacterEncodingFilter</filter-class>
    <init-param>
      <param-name>encoding</param-name>
      <param-value>UTF-8</param-value>
    </init-param>
    <init-param>
      <!-- บังคับแม้ browser จะส่ง Content-Type อื่นมา -->
      <param-name>ignore</param-name>
      <param-value>false</param-value>
    </init-param>
  </filter>
  <filter-mapping>
    <filter-name>EncodingFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <!-- ====================================================
       WELCOME FILE
       ==================================================== -->
  <welcome-file-list>
    <welcome-file>index.jsp</welcome-file>
  </welcome-file-list>

  <!-- ====================================================
       SESSION CONFIG
       ==================================================== -->
  <session-config>
    <session-timeout>60</session-timeout>
  </session-config>

  <!-- ====================================================
       JSP CONFIG — บังคับ UTF-8 สำหรับทุกหน้า JSP
       ==================================================== -->
  <jsp-config>
    <jsp-property-group>
      <url-pattern>*.jsp</url-pattern>
      <page-encoding>UTF-8</page-encoding>
      <trim-directive-whitespaces>true</trim-directive-whitespaces>
    </jsp-property-group>
    <jsp-property-group>
      <url-pattern>*.jspf</url-pattern>
      <page-encoding>UTF-8</page-encoding>
    </jsp-property-group>
  </jsp-config>

  <!-- ====================================================
       MULTIPART CONFIG สำหรับ upload.jsp
       รองรับไฟล์ชื่อภาษาไทย ขนาดสูงสุด 50MB
       ==================================================== -->
  <servlet>
    <servlet-name>upload</servlet-name>
    <jsp-file>/upload.jsp</jsp-file>
    <multipart-config>
      <location>/tmp</location>
      <max-file-size>52428800</max-file-size>       <!-- 50 MB -->
      <max-request-size>57671680</max-request-size> <!-- 55 MB -->
      <file-size-threshold>1048576</file-size-threshold> <!-- 1 MB -->
    </multipart-config>
  </servlet>
  <servlet-mapping>
    <servlet-name>upload</servlet-name>
    <url-pattern>/upload.jsp</url-pattern>
  </servlet-mapping>

</web-app>
